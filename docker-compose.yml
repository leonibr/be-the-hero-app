version: '2.0'
services:
  frontend:
    build: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    links:
      - backend
  backend:
    restart: always
    build: backend
    ports: 
      - "127.0.0.1:3333:3333"
    
